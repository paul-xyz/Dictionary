catalog = []

def book(title, author, genre):
    book_add = input ("Do you want to put this book in the catalog? ")
    if book_add == ("yes, y, Yes"):
        catalog.append[]:
        return "Book has been added to the catalog, call the variable catalog to view books that are in the catalog."
    else:
        return;
